
void main()
{

  int i;

  i = 200;

  while(i >= 100)
    cand(i >= 100)
    cand(i > 0)
    cand(i >= 98)
    cand((i % 2) == 0)
    cand((i % 2) == 1)
    cand(i == 17)
  {
    i = i - 2;
    assert(i > 0);
  }
  assert(i > 0);

}
